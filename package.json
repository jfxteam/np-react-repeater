{
  "author": {
    "name": "Anton Lebedev @antoinedze"
  },
  "bugs": {
    "url": "https://github.com/wf-team/npm-react-repeater/issues"
  },
  "description": "JavaScript React repeater module for cloning ui elements",
  "homepage": "https://github.com/wf-team/npm-react-repeater",
  "license": "MIT",
  "main": "index.js",
  "name": "@wf-team/react-repeater",
  "scripts": {
    "deploy": "npm i add-dependencies -g && npm i npm-check-updates -g",
    "fix-start": "ncu -u -n \"/^@wf-team.*$/\" && npm --no-git-tag-version version pre%fix-type% && npm publish",
    "fix": "ncu -u -n \"/^@wf-team.*$/\" && npm --no-git-tag-version version prerelease && npm publish",
    "fix-end": "ncu -u \"/^@wf-team.*$/\" && npm --no-git-tag-version version %fix-type% && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wf-team/npm-react-repeater.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/wf-team"
  },
  "dependencies": {
    "@wf-team/events-handler": "^0.0.1-0",
    "react": "^16.12.0"
  },
  "version": "0.0.1-0"
}
